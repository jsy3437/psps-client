import React from 'react';
import { withRouter } from 'react-router-dom';

const RegisterPage = () => {
	return <div id='container'>RegisterPage...</div>;
};

export default withRouter(RegisterPage);
